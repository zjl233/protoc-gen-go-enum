syntax = "proto3";

package example;

option go_package = "github.com/nixihz/protoc-gen-go-enum/example";

// 平台枚举值
enum PLATFORM{
  PLATFORM_UNKNOWN = 0;
  // Android
  ANDROID = 1;
  // iOS
  IOS = 2;
}
